# HTTP Headers: {"x-tenant-id": "coffee-roaster-demo"}
query OurTenant {
    tenant {
        id
        name
        normalizedName
        settings {
            mqtt {
                url
                username
                password
            }
        }
        existingSignalTypes
    }
}
